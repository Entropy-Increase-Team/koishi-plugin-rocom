# RoCom 4.1 对齐问题修复记录

日期：2026-09-21。对应 [修复前验收报告](rocom-4.1-acceptance-review-2026-09-21.md)。

## 结果

报告中的 R01–R12 已落实到代码。原 27 项检查全部通过，增加失败轮询、初次请求超时、旧业务对象兼容、夏令时、取消与重试、公告权限等检查后，当前 **44/44 通过**。

真实 Chrome 对六类模板完成截图验证：排行榜 50 名分 2 张，长公告分 3 张，分享码、公告列表、玩家和查蛋各 1 张。分页连续、无极短尾页，所有渲染创建的 page 已释放。浏览器测试拦截全部页面 HTTP(S) 请求，未使用真实用户数据或发送聊天消息。

## 修复对应关系

| 编号 | 已完成修改 | 主要位置 |
| --- | --- | --- |
| R01 | 先处理失败/排队状态再提取数据；多层解包保留映射；缺 task_id、完成却缺数据报错；兼容旧 rows 与业务 result 共存 | `src/client.ts` |
| R02 | 使用订阅指定 self_id 的 Bot 定向发送；旧订阅仅在平台 Bot 唯一时兼容 | `src/subscription-send.ts` |
| R03 | 非空消息 ID 才算成功；图片失败尝试文字，失败不推进状态 | 发送封装与三个订阅模块 |
| R04 | 三类订阅分别互斥；取消贯穿请求/渲染/发送；删除或修改订阅后不写回旧快照 | `src/subscription-runner.ts`、命令模块 |
| R05 | 优先 isDirect，再读规范化频道类型；缺 guildId 的群不作为私聊免权限 | `src/permissions.ts` |
| R06 | 统一读取 session.user.authority，显式加载字段；角色精确匹配；公告保存 self_id | 权限服务、用户模型、命令模块 |
| R07 | 玩家/名片允许仅 Token；家园入口传 auth；提交和轮询共享截止时间 | `src/client.ts`、`src/async-control.ts`、`query.ts` |
| R08 | 新 card 与旧 rows 合并；头像字段补正确位置；检查角色一致性；玩家图文展示收集/名片信息 | 玩家服务与模板 |
| R09 | API/本地查蛋图文展示体型；空范围不转成 0；家园详情共用判定 | `pet-size.ts`、两个精灵服务、查蛋模板 |
| R10 | 配种总量取合并蛋组 total；总量不可用时标为“当前预览数” | `src/egg-service.ts` |
| R11 | 总预算覆盖 page 获取到截图，超时主动关闭；全局配置生效；长图分页均分尾页 | `src/render.ts`、异步控制与入口 |
| R12 | 分享码文字补天赋，历史解析失败明确报错；排行用“该玩家名次” | 分享码/排行服务及模板 |

同时补齐公告图文和长文分页、商人空结果最多三次重试、定时 ±30 秒抖动、实例时区隔离及夏令时日界、家园绑定昵称/@提醒。重复家园订阅保留通知状态。命令文档和 README 已同步。

## 验证

| 检查 | 结果 |
| --- | --- |
| `npm run check` | 通过 |
| `npm run build` | 通过，含资源复制 |
| `npm run test:alignment` | 44/44 通过 |
| `node tests/render-smoke.cjs` | 六类截图通过，Chrome 151.0.7922.138 |
| `git diff --check` | 通过 |
| `npm pack --dry-run --ignore-scripts --json` | 通过，341 个文件；新模板入包，tests/参考项目未入包 |

最终打包预览：压缩 19,954,237 字节，解包 31,142,916 字节。未发布包。浏览器沙箱内启动超时后，获准在沙箱外运行本地检查并通过；npm 缓存目录同样需要沙箱外权限。

复现与结果：[逻辑测试](../tests/alignment-review.cjs)、[逻辑结果](alignment-review-results.json)、[浏览器测试](../tests/render-smoke.cjs)、[浏览器结果](render-review-results.json)。截图写入系统临时目录。

## 剩余边界

生产后端接口权限、真实商品映射/时间单位、服务端仅 Token 查询，以及官方 QQ/OneBot 实际角色事件和消息投递仍需部署环境联调。本次没有调用生产 API 或发送真实聊天消息。远程图片和消息平台尺寸上限也需要实服确认。

字体外置继续按原计划独立安排，保留现有字体；未调整插件版本号或发布 npm。原验收报告保留修复前事实，当前状态以本记录和重新执行的测试结果为准。
